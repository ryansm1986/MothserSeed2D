Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/east`.

Required source reference: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\east.png`.

Use the visible east base image as the strict source of truth. Create the same exact east-facing tree goblin monster in five newly drawn idle poses. Preserve bark-and-root anatomy, branch crown, leafy back/shoulder mass, green glowing eye, open wooden mouth, clawed wooden hands, root feet, green hanging moss/vines, golden vine belt, vine rings, tan/brown bark palette, green leaf palette, black pixel outline, and imposing tree-creature silhouette.

Create exactly 5 newly drawn pixel-art poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `east` direction only, exactly 5 frame lanes, no other rows or directions.

Use a very wide panoramic horizontal canvas with huge empty `#ff00ff` gutters. Leave at least `768px` of empty flat `#ff00ff` between neighboring visible pose bounds if possible, and never less than `384px`. Leave large margins before the first pose and after the last pose. Prefer wider canvas over smaller poses. Do not shrink, crop, simplify, or redesign the monster to fit.

Frame plan:
1. Neutral grounded east-facing pose close to the base.
2. Inhale/lift: head, upper trunk, and branch tips rise slightly.
3. Gentle forward sway: head and shoulders lean a little toward the right-facing direction, arms lag.
4. Exhale/settle: head dips, shoulders and hanging moss lower.
5. Return-ready counter-sway, close enough to loop to frame 1 but not duplicated.

Do not add humanoid goblin details, elf ears, skulls, pouches, clothing, armor, jewelry, weapons, shields, props, extra heads, extra limbs, labels, text, borders, scenery, reference panels, or grid lines.
