Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/west`.

Required source reference: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\west.png`.

Use the visible west base image as the strict source of truth. Create the same exact west-facing tree goblin monster in five newly drawn idle poses. Preserve bark-and-root anatomy, side-facing hornlike branch brow, branch crown, glowing green eye, angular wooden face with open dark mouth and teeth, broad forward branch arms, clawed wooden hands, leafy back/shoulder mass, green hanging moss/vines, golden vine belt, vine rings, root feet, branch sprouts and leaves.

Create exactly 5 newly drawn pixel-art poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `west` direction only, exactly 5 frame lanes, no other rows or directions. Use `768px` preferred empty gutters and `384px` hard minimum between visible pose bounds.

Frame plan: neutral west pose; inhale/lift; gentle forward sway; exhale/settle; counter-sway/return-ready. Do not add humanoid goblin details, props, labels, text, borders, scenery, reference panels, or grid lines.
