Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/southeast`.

Required source references: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\south.png` and `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\east.png`.

Use the visible south and east base images as strict source-of-truth references. The generated subject must be the same tree goblin monster, turned to the `southeast` diagonal between south/front and east/right. Preserve the bark-and-root anatomy, branch crown, leafy shoulder/crown mass, green glowing eyes, clawed wooden hands, root feet, green hanging moss/vines, golden vine belt, vine rings, tan/brown bark palette, green leaf palette, black pixel outline style, and imposing tree-creature silhouette.

Create exactly 5 newly drawn pixel-art idle poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `southeast` direction only, exactly 5 frame lanes, no other rows or directions.

Use a very wide panoramic horizontal canvas with huge empty `#ff00ff` gutters. Leave at least `768px` of empty flat `#ff00ff` between neighboring visible pose bounds if possible, and never less than `384px`. Leave large margins before the first pose and after the last pose. Prefer wider canvas over smaller poses. Do not shrink, crop, simplify, or redesign the monster to fit.

Every frame must be a distinct newly drawn idle pose. Keep root feet mostly planted and make subtle readable motion through head, upper trunk, branch arms, leafy shoulder/crown mass, hanging moss, and branch tips.

Frame plan:
1. Neutral grounded southeast-facing pose between the south and east bases.
2. Inhale/lift: head and upper trunk lift slightly, branch tips rise, leafy shoulders lift.
3. Gentle sway toward screen-left/front: head/crown lean subtly, branch arms lag, root stance remains planted.
4. Exhale/settle: head dips, shoulders and moss lower, branch tips relax.
5. Gentle sway toward screen-right/back and return-ready pose, close enough to loop to frame 1 but not duplicated.

Do not add humanoid goblin details, elf ears, skulls, pouches, clothing, armor, jewelry, weapons, shields, props, extra heads, extra limbs, labels, text, borders, scenery, reference panels, or grid lines.
