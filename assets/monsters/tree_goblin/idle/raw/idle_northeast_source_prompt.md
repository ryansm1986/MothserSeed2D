Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/northeast`.

Required source references: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\north.png` and `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\east.png`.

Use the visible north and east base images as strict source-of-truth references. The generated subject must be the same tree goblin monster, turned to the `northeast` diagonal between north/back and east/right. Preserve the bark-and-root anatomy, branch crown, leafy back/shoulder mass, clawed wooden hands, root feet, green hanging moss/vines, golden vine belt, vine rings, tan/brown bark palette, green leaf palette, black pixel outline style, and imposing tree-creature silhouette.

Create exactly 5 newly drawn pixel-art idle poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `northeast` direction only, exactly 5 frame lanes, no other rows or directions.

Use a very wide panoramic horizontal canvas with huge empty `#ff00ff` gutters. Leave at least `768px` of empty flat `#ff00ff` between neighboring visible pose bounds if possible, and never less than `384px`. Do not shrink, crop, simplify, or redesign the monster to fit.

Frame plan:
1. Neutral grounded northeast-facing pose between the north and east bases.
2. Inhale/lift: upper trunk and branch tips rise slightly.
3. Gentle sway toward screen-left/back; leafy back mass and branches lag subtly.
4. Exhale/settle: shoulders, branch tips, and moss lower.
5. Gentle counter-sway toward screen-right/front, close enough to loop to frame 1 but not duplicated.

Do not add humanoid goblin details, skulls, pouches, clothing, armor, jewelry, weapons, shields, props, extra heads, extra limbs, labels, text, borders, scenery, reference panels, or grid lines.
