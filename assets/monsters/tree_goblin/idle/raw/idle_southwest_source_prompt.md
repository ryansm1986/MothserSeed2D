Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/southwest`.

Required source references: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\south.png` and `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\west.png`.

Use the visible south and west base images as strict source-of-truth references. The generated subject must be the same tree goblin monster, turned to the `southwest` diagonal between south/front and west/left. Preserve bark-and-root anatomy, branch crown, leafy shoulder/crown mass, green glowing eyes, clawed wooden hands, root feet, green hanging moss/vines, golden vine belt, vine rings, tan/brown bark palette, green leaf palette, black pixel outline style, and imposing tree-creature silhouette.

Create exactly 5 newly drawn pixel-art idle poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `southwest` direction only, exactly 5 frame lanes, no other rows or directions. Use `768px` preferred empty gutters and `384px` hard minimum between visible pose bounds.

Frame plan: neutral southwest pose; inhale/lift; slight front-left sway; exhale/settle; slight counter-sway/return-ready. Do not add humanoid goblin details, mushroom features, props, labels, text, borders, scenery, reference panels, or grid lines.
