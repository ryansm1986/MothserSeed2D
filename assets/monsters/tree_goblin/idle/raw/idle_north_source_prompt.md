Use `$game-studio:sprite-pipeline`.

Generate exactly one fresh horizontal source strip for exactly one animation and one direction: `idle/north`.

Required source reference: `D:\projects\MotherSeed2D\assets\monsters\tree_goblin\base\north.png`.

Use the visible north base image as the strict source of truth. Create the same exact north/back-facing tree goblin monster in five newly drawn idle poses. Preserve bark-and-root anatomy, branch crown, leafy back/crown mass, broad branch arms, clawed wooden hands, root feet, green hanging moss/vines, golden vine belt, vine rings, tan/brown bark palette, green leaf palette, black pixel outline, and imposing tree-creature silhouette.

Create exactly 5 newly drawn pixel-art poses on a perfectly flat solid `#ff00ff` chroma-key background. Output one single ultra-wide horizontal strip only, `north` direction only, exactly 5 frame lanes, no other rows or directions. Use a very wide panoramic canvas with `768px` preferred empty gutters and `384px` hard minimum between visible pose bounds.

Frame plan: neutral north pose; inhale/lift; slight sway left/back; exhale/settle; slight sway right/return-ready. Do not add humanoid goblin details, props, labels, text, borders, scenery, reference panels, or grid lines.
